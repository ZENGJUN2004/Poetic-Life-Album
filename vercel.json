{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "export DATABASE_URL=\"${DATABASE_URL:-${POSTGRES_PRISMA_URL:-${POSTGRES_URL}}}\" && prisma generate && prisma db push --accept-data-loss && next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "regions": ["hnd1"],
  "functions": {
    "app/api/**/*.{ts,tsx,js,jsx}": {
      "maxDuration": 60
    }
  }
}
